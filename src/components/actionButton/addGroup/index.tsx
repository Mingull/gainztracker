import { Text } from "@/components";
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AddGroup() {
	return (
		<SafeAreaView>
			<View>
				<Text variant={"title"}>Add Group</Text>
			</View>
		</SafeAreaView>
	);
}
